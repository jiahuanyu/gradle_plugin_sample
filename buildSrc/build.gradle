apply plugin: 'java' // groovy: apply plugin: 'groovy'

dependencies {
    implementation gradleApi()// gradle sdk
    implementation 'com.android.tools.build:gradle:3.2.1'// Transform api
    implementation 'org.ow2.asm:asm:6.0' // ASM
    implementation 'org.ow2.asm:asm-commons:6.0'
}

repositories {
    google()
    jcenter()
}
